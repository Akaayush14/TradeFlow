<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".TradeFlowApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TradeFlow"
        tools:replace="android:fullBackupContent">

        <!-- ThemeWrapper from your code -->
        <activity
            android:name=".view.ThemeWrapper"
            android:exported="true"
            android:label="@string/title_activity_theme_wrapper"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LocationPickerActivity from friend's code -->
        <activity
            android:name=".view.LocationPickerActivity"
            android:exported="true"
            android:label="@string/title_activity_location_picker"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MapActivity from friend's code -->
        <activity
            android:name=".view.MapActivity"
            android:exported="true"
            android:label="@string/title_activity_map"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Google Maps API Key from friend's code -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCX3_5f2eilotR-J9y7sC-7Q-zCFP98G7U" />

        <!-- MainActivity (both have it) -->
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SplashActivity (both have it) -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:label="@string/title_activity_splash"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- LoginActivity (both have it) -->
        <activity
            android:name=".view.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ForgetPasswordActivity (both have it) -->
        <activity
            android:name=".view.ForgetPasswordActivity"
            android:exported="true"
            android:label="@string/title_activity_splash"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- RegisterActivity (both have it) -->
        <activity
            android:name=".RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- UserDashboard (both have it, friend has extra meta-data) -->
        <activity
            android:name=".view.UserDashboard"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- UserItemDetails (both have it) -->
        <activity
            android:name=".view.UserItemDetails"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- UserSetting (both have it) -->
        <activity
            android:name=".view.UserSetting"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- UserChatSystemActivity (both have it) -->
        <activity
            android:name=".UserChatSystemActivity"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdminDashExp (both have it) -->
        <activity
            android:name=".view.AdminDashExp"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdminDashUser from friend's code -->
        <activity
            android:name=".view.AdminDashUser"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdminProfile (both have it) -->
        <activity
            android:name=".view.AdminProfile"
            android:exported="true"
            android:label="Admin Profile"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AdminNotification (both have it) -->
        <activity
            android:name=".view.AdminNotification"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Common activities (both have these) -->
        <activity
            android:name=".view.AdminUserMetric"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminAdminMetric"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminProductMetric"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminProductPriceMetric"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminAdmin"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminSettings"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.EditAdminProfile"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminAboutUs"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminPrivacyPolicy"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminTermsAndCondition"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminDashItem"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.UserPointsActivity"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.UserProfileActivity"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminPointDeals"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.BarterRequestActivity"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.RentalRequestActivity"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />

        <!-- Unique activities from your code -->
        <activity
            android:name=".view.AdminItemDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />
        <activity
            android:name=".view.AdminUserDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.TradeFlow" />

        <!-- ChatActivity from your code -->
        <activity
            android:name=".view.ChatActivity"
            android:exported="true"
            android:theme="@style/Theme.TradeFlow"
            android:windowSoftInputMode="adjustResize">
        </activity>
    </application>
</manifest>